pipeline {
    agent any
    //「Global Tool Configuration」で設定したnameを指定
    stages {
        stage('Backend Deploy to Apache') {
            steps {
                sh '''
                rsync -av --no-owner --no-group backend/ /api/
                sudo systemctl restart fastapi
                '''
            }
        }
    }
}
